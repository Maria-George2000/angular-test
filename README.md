# AngularTest

A simple Angular example showing how to pass **Reactive Form** data from a parent to a child component using `@Input`.

## Setup & Run

```bash
# Clone repo
git clone https://github.com/your-username/your-repo.git
cd your-repo

# Install dependencies
npm install

# Start development server
ng serve
